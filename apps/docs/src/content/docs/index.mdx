---
title: tailnet.ts
description: One TypeScript client for Tailscale and Headscale control planes, plus a client for the local daemon.
template: splash
hero:
  tagline: Same code against Tailscale or Headscale. A separate, honest client for the daemon on this machine. Every call that can hurt says so where you read it.
  actions:
    - text: Get started
      link: /start/installation/
      icon: right-arrow
    - text: Reference
      link: /reference/tailnet/
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components"

<CardGrid stagger>
  <Card title="One contract, two backends" icon="random">
    `tailnet.ts` defines what a control plane can do. `headscale.ts` and
    `tailscale.ts` implement it. Swap the backend, keep the code.
  </Card>
  <Card title="Risk lives in the docs" icon="warning">
    No namespaces to remember. Each method's `@remarks` says whether it reads,
    writes or destroys, and your editor shows it on hover.
  </Card>
  <Card title="Grounded in real servers" icon="approve-check">
    Headscale types came from a running server, Tailscale types from its
    OpenAPI schema. Where the two APIs disagree, the docs say so.
  </Card>
  <Card title="Test without your tailnet" icon="rocket">
    The Nix dev shell boots a throwaway headscale and tailscaled on loopback,
    so destructive calls never touch a network you care about.
  </Card>
</CardGrid>
